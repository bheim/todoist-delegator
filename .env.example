# Required
TODOIST_API_TOKEN=
ANTHROPIC_API_KEY=

# Optional (defaults shown)
# CHROME_PROFILE_PATH=  # Auto-detected per OS. Override if needed.
DELEGATE_LABEL_NAME=delegate
POLL_INTERVAL_SECONDS=30
AGENT_MODEL=sonnet
AGENT_MAX_TURNS=50
WORKING_DIR=./agent-workspace
